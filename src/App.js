import makeStyles from '@material-ui/core/styles/makeStyles'

const useStyles = makeStyles(theme => ({
}))

function App() {
  const {} = useStyles()

  return (
    <div></div>
  )
}

export default App;
